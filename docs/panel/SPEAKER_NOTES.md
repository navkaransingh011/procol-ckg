# Speaker notes

## Slide 1: Ask the codebase.

Open with the one-line promise: every answer names the file, the guide or the live row that proves it, and says so when it cannot tell. Then say the deck is honest about what is not built yet: slides 20 to 22.

## Slide 3: One graph, one database, one agent

Walk left to right. Stress: the model never writes structural facts; it only reads what the service hands it.
